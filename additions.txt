🔷 🔧 Кросс-главные дополнения к архитектуре с учётом ограничений
✅ Модель критичности функционала для клиента

Где учесть:

    Вводится в Гл. 1.1 "Отказоустойчивая разработка", а также в Гл. 5 "Политики".

Что добавить:

    Разделение функционала на 3 класса:

        Критичный (Tier-1) — оформляет транзакции, продажи, учётные записи. Не может падать.

        Средний приоритет (Tier-2) — сервисы уведомлений, рекомендательные модули.

        Фоновый/необязательный (Tier-3) — аналитика, отчёты, несущественные интеграции.

Меры:

    Tier-1 — максимальная изоляция и резервирование.

    Tier-2 — Graceful Degradation.

    Tier-3 — допускается временное отключение.

    📎 Пример: при перегрузке системы Tier-1 обслуживается первым, остальные откладываются.

✅ Периоды пиковой нагрузки

Где учесть:

    В Гл. 2.5 "Стратегии обновлений", Гл. 4.3 "Планирование ёмкости", Гл. 5.3 "Антикризисные политики".

Что добавить:

    Планируем временные окна ограничений на релизы (release freeze) в периоды пиков.

    Используем Horizontal Pod Autoscaler + Cluster Autoscaler с предзагрузкой.

    Метрики прогнозной нагрузки — на основе исторических трендов (сезонность).

    A/B нагрузочное тестирование до наступления пикового сезона.

    📎 Пример: для интернет-магазина — усиление кластера и ограничения на выкатку новых фич за 1 неделю до Чёрной пятницы.

✅ Постоянно растущий массив данных (удваивается ежегодно)

Где учесть:

    В Гл. 3.1 "Хранение", Гл. 3.2 "Репликация", и Гл. 2.4 "Вычислительная инфраструктура".

Что добавить:

    Разделение на Hot/Cold зоны хранения:

        Hot (оперативный доступ) — SSD, shard-based PostgreSQL, горизонтально масштабируемые NoSQL-хранилища (Tarantool/Redis).

        Cold — объекты (MinIO, S3-совместимые), архивы.

    Использовать подход sharding + partitioning.

    Применять таблицы с автоархивацией (PostgreSQL declarative partitioning + cron).

    Планируем Data Retention Policy, реализуемое на уровне БД и CI/CD.

    📎 Пример: активные заказы и клиенты — в hot-хранилище; старые заказы — в архивах (S3).

✅ Данные с редким доступом, срок хранения до 5 лет

Где учесть:

    В Гл. 3.1 "Уровни хранения", дополнительно в Гл. 5.1 "DR-планы".

Что добавить:

    Архивные данные отправляются в Cold Storage: S3, MinIO, Glacier-аналоги.

    Для систем 1–2 тира — реализуем доступ через API-запрос к объектному хранилищу.

    Используем раз в сутки / в неделю ETL-выгрузки, без постоянного присутствия в RAM/DB.

    📎 Пример: бухгалтерские документы 3-летней давности запрашиваются через отчёт с задержкой 10–15 сек.

✅ Срок хранения логов — 30 дней

Где учесть:

    В Гл. 4.2 "Централизованное логирование", Гл. 5.1 "Резервное копирование".

Что добавить:

    Используем лог-аггрегаторы с ротацией: Loki, ELK, Fluent Bit + Logrotate.

    Настроить retention.days = 30, retention.size = X GB.

    Запуск задач на ежедневную очистку логов, либо хранение в S3 с tag-based удалением.

    Хранение аудит-логов отдельно от технических логов.

    📎 Пример: технические логи контейнеров хранятся 30 дней, логи входов и транзакций — 90 дней в cold хранилище.

✅ Эффективность хранения по объёму и стоимости

Где учесть:

    В Гл. 3.1 + таблице выбора технологии по затратам, также — в приложении с рекомендациями по технологии.

Что добавить:

    Расчёт TCO хранения по слоям: SSD, SATA, S3/Glacier.

    Гибридное хранение:

        SSD для hot (до 15% от общего объёма)

        HDD / облачные хранилища — для cold

    Использование сжатия (gzip, zstd) на уровне БД, логов, архивов.

    Оценка стоимости 1 ТБ в год по каждому типу хранения.

    📎 Пример: архивы логов — в zstd формате в S3; online данные — PostgreSQL с TOAST-сжатием и pg_partman.